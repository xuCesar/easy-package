{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Easy Package 主窗口的最小权限",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
